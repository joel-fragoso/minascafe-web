import { FC } from 'react';
import './styles.css';
import Accordion from '../Accordion';

const Main: FC = () => {
  return (
    <main className="main-content">
      <Accordion />
    </main>
  );
};

export default Main;
